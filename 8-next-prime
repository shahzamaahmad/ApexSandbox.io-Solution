public Integer nextPrime(Integer num) {
    Integer nextNum = num + 1;
        while (true) {
            if (Math.mod(nextNum,2)!=0)) {
                return nextNum;  
            }
            nextNum++;
        }
     return nextNum;  
}