public String formatName(String firstName, String lastName) {
    if(String.IsEmpty(firstName)) {
        return lastName;
    }
    if(String.IsEmpty(lastName)) {
        return firstName;
    }
   
    if(String.IsNotBlank(firstName) && String.IsNotBlank(firstName)) {
        return lastName + ', ' +firstName ;
    }
    return lastName + ', ' +firstName ;