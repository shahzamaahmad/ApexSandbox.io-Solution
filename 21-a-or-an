public String aOrAn(String word) {
    // Define vowels directly as a string for easy checking
   // String vowels = 'aeiou';
    // Check if the word starts with any of the vowels
  //  if (vowels.contains(word.substring(0, 1))) {
  //      return 'an ' + word;
   // }
  //  return 'a ' + word;
//}
//public String aOrAn(String word) {
   List<String> words = new List<String>{'o', 'i', 'a','e','u'};
    for(String word2 : words){
        if(word.startsWith(word2)){
            return 'an '+word;
        }
    }
    return 'a '+word ;

    
}