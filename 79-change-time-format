public String changeTimeFormat(String strTime) {
    if(String.isNotBlank(strTime)){
        List<String> tme = new List<String>({
            strTime.split(':');
        });
        //tme.add = strTime.split(':');
        return  tme;


    }
    return 'Invalid Time';
}